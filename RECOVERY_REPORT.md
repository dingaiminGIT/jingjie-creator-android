# CoCamera 功能恢复与 Android 实现报告

## 信息来源与结论

分析对象是 USB 连接的 iPhone 上已安装的 CoCamera 2.3.3（Bundle ID `com.recordtoolvideo.camera`），辅以用户提供的当前界面截图及 App Store 公开说明。由于设备是非越狱 iOS，且 App Store 安装包没有调试授权，无法提取原工程或附加调试器；本项目因此采用“行为核对 + 独立重写”，而不是复制原二进制实现。

已确认产品核心不是普通相机，而是两层能力：

1. 拍摄工作台：单摄、前后双摄、画中画/分屏、提词、水印、预录、内容讲解。
2. 悬浮创作：把提词、镜头或跟拍素材悬浮到微信、浏览器、视频等其他 App 上方继续创作。

## 原产品功能与付费边界

公开说明和当前截图合并后，可确认以下功能：

| 模块 | 功能 |
| --- | --- |
| 悬浮创作 | 录屏讲解、悬浮提词、视频跟拍、图集跟拍 |
| 场景 | 旅行双摄、赛事同框、15 秒预录、提词拍摄、现场讲解、内容讲解、快速拍摄 |
| 相机 | 前后摄切换、后置广角+超广角、前后画中画/分屏、变焦、对焦、录制中拍照 |
| 创作工具 | 提词速度/字号/颜色/旋转、语音跟读、水印、参考图滤镜、贴纸和前景抠图 |
| 素材 | 图片、视频、文件和网页导入，浏览/播放时讲解录制 |
| 作品 | 视频/照片/滤镜图管理、分享导出、多音轨合并 |

会员权益是：导出不限时长、导出无水印、播放页可隐藏顶部和底部栏。非会员公开限制是导出最多 30 秒且带水印；提供周、月、年订阅和一次性终身会员。

## Android 0.3.1 实现矩阵

| 能力 | 状态 | 说明 |
| --- | --- | --- |
| 首页与场景导航 | 已完成 | 保留 7 个有默认工具/布局差异的场景，赛事与旅行合并为运动·旅行双摄 |
| 单摄拍照/录像 | 已完成 | CameraX，FHD 优先，H.264 + AAC，保存到系统媒体库 |
| 变焦/对焦/补光/网格 | 已完成 | 拍摄页直接操作 |
| 前后双摄同框 | 已完成 | 合成 JPEG/MP4；三种布局可视化选择；全布局主副互换；录像中可实时切换 |
| 画中画拖动 | 已完成 | 预览位置会按屏幕归一化坐标写入合成照片与视频 |
| 机内提词 | 已完成 | 文稿编辑、速度调节、自动滚动、系统语音识别跟读 |
| 悬浮提词 | 已完成 | Android Overlay + 前台服务，可跨 App、拖动、暂停、关闭 |
| 时间/地名/方位水印 | 已完成 | GPS/网络定位择优 + 反向地理编码，不显示经纬度，可手动刷新，直接烘焙至照片/视频 |
| 15 秒预录 | 已完成 | 3×5 秒滚动缓冲，按下后与当前录像无损拼接 |
| 网页/课件录屏 | 已完成 | MediaProjection + H.264/AAC，通知栏停止并保存；首次必须由用户确认系统录屏授权 |
| 视频/图集跟拍 | 已完成 | 本地素材选择 + Android Overlay 前台服务；视频支持播放控制、变速、循环、镜像、静音，图集支持切换与完成标记 |
| 参考图滤镜/抠图/贴纸 | 待实现 | 需 GPU 特效和分割模型 |
| 双摄合成录像 | 已完成 | CameraX RGBA 双路采集 + MediaCodec H.264/AAC 合成，水印同管线写入；音视频使用共同单调时钟且 PCM 不丢帧 |

Android 重写版没有会员逻辑，当前已经完成的功能没有 30 秒限制，也不会主动添加会员水印。这是独立实现，不会修改或规避 iOS 原 App 的购买状态。

## 真机验证

测试设备：Xiaomi `2410DPN6CC`，Android 16，1080×2400。

- APK 构建、覆盖安装和冷启动通过，未出现 Java 崩溃。
- 后置单摄 CameraX Preview、ImageCapture、VideoCapture 绑定成功。
- 普通单摄实际生成 MP4：1920×1080，约 30fps，H.264 + 48kHz 双声道 AAC。
- 设备前后摄并发成功：后摄全屏、前摄画中画同时出画。
- 双摄三种布局预览通过；上下均分合成照片为 1080×1920 JPEG。
- 上下均分实际录像为 720×1280、24fps、H.264 + 44.1kHz 单声道 AAC，时长 5.875 秒。
- 画中画拖至左侧后，合成 JPEG 和 MP4 均保留左侧坐标。
- 同一段 5.917 秒双摄 MP4 内成功完成录像中主副镜头互换，无中断、无分文件。
- 15 秒预录真机样本为 13.511 秒、1080P H.264 + 48kHz 双声道 AAC，可正常解析。
- 定位水印实际显示为“北京市 · 通州区 · 经海七路”，未暴露经纬度。
- 小米系统语音服务真机返回 available/ready，无语音时正常返回 no-match 并继续监听；跟读改为识别文本与文稿逐段匹配。
- 语音跟读与 CameraX 录像同时开启 6.429 秒，识别服务持续 ready，成片保留 48kHz 双声道 AAC（非静音）。
- 跨 App 悬浮提词成功显示在系统桌面上，前台服务保持运行。
- 视频跟拍使用 4 秒测试片在其他 App 上方连续播放；暂停、0.5 倍速、镜像、静音、循环开关和三档缩放均真机生效。
- 图集跟拍在微信上方加载 2 张参考图；前后切换、完成/取消完成、计数和三档缩放均真机生效。
- 修复前 4 个双摄样本的视频轨分别比音频轨长 12.055、8.881、3.452、4.629 秒；修复后 35.98 秒样本的音视频末尾仅差 39 毫秒，起点差 10.6 毫秒。
- 双摄实时合成录像期间采样 2011 帧：`gfxinfo` 报告 2 个卡顿帧（0.10%），50/90/95/99 分位耗时分别为 5/5/5/7ms。

## 仍可扩展的能力

1. 预录的离线语音口令触发。
2. 参考图滤镜、前景抠图、贴纸和多音轨合并。
3. 更长时间录制、温升、前后台恢复和更多厂商兼容性测试。
